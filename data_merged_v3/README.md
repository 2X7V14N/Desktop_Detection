# 合并后的桌面物体检测数据集

这是本项目用于训练和测试的 YOLO 数据集，类别编号固定为：

```text
0: cup
1: book
2: pen
```

数据划分如下：

- `images/train`、`labels/train`：训练集
- `images/val`、`labels/val`：验证集
- `images/test`、`labels/test`：最终测试集

数据集由多个公开数据集整理而来。原始数据集的来源、许可证和具体版本以
`public_datasets` 中的记录及 README 中的链接为准。
